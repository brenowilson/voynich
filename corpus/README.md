# Canonical Independent Corpus

The corpus is built directly from primary images and stores observations rather than meanings.

Core entities: page images, regions, line candidates, graphical components, glyph-instance candidates, provisional visual classes, ordered sequences, alternatives and uncertainty.

External identifiers may appear only in separately generated mapping files under `external-comparisons/`.
