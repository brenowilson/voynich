# Page Registration Protocol

Register each image to an institutional folio identifier. Record crop, rotation, perspective correction, scale and masks as explicit transforms. Never overwrite the source image. Foldouts and multi-panel images must retain panel relationships.
