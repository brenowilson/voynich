# Glyph Segmentation Protocol

Segmentation produces candidates, not letters.

For each candidate record bounding geometry, connected components, neighboring marks, touching/overlap state, segmentation method, confidence and alternative splits or merges. Ambiguity must remain representable.
