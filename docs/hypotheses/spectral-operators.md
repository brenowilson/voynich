# Spectral Operator Hypotheses

This document is a registry, not an assertion that any particular operator is natural.

Candidate families include sequential transition operators, spatial neighborhood operators, recurrence operators, graph Laplacians, multiscale operators and operators induced by geometric features.

Each candidate must define data units, construction rule, normalization, boundary conditions, expected invariances, null models and perturbation tests before interpretation of its spectrum.
